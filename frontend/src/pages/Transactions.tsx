import styled from "styled-components";

const Transactions = () => {
  return (
    <TransactionsStyle>
      <h1>Transactions</h1>
    </TransactionsStyle>
  );
};

const TransactionsStyle = styled.div``;

export default Transactions;
