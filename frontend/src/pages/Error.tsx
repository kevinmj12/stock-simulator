import styled from "styled-components";

const Error = () => {
  return (
    <ErrorStyle>
      <h1>오류가 발생하였습니다</h1>
    </ErrorStyle>
  );
};

const ErrorStyle = styled.div``;

export default Error;
